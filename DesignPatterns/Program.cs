using System;
using DesignPatterns.Decorator;
using DesignPatterns.Strategy;
using DesignPatterns.Observer;
using DesignPatterns.Singleton;
using DesignPatterns.Prototype;
using DesignPatterns.Adapter;
using DesignPatterns.Builder;
using DesignPatterns.Factory;
using DesignPatterns.Bridge;
using DesignPatterns.Composite;
using DesignPatterns.Facade;
using DesignPatterns.Flyweight;
using DesignPatterns.Proxy;
using DesignPatterns.Chain_of_Responsibility;
using DesignPatterns.Command;
using DesignPatterns.Interpreter;
using DesignPatterns.Iterator;
using DesignPatterns.Mediator;
using DesignPatterns.Memento;
using DesignPatterns.State;
using DesignPatterns.Visitor;
using DesignPatterns.TemplateMehod;


namespace DesignPatterns
{
    class Program
    {
        static void Main(string[] args)
        {
            //********Odkomentuj odpowiedni wzorzec projektowy 

            //var decorator = new DecoratorRun();  //Decorator
            //var strategy = new StrategyRun(); //Startegy
            //var observer = new ObserverRun();  // Observer
            //var singleton = new SingletonRun();  //Singleton
            //var prototype = new PrototypeRun();   //Prototype 
            //var adapter = new AdapterRun();     //Adapter
            //var builder = new BuilderRun();    //Builder
            //var factory = new FactoryRun();    //Factory
            //var bridge = new BridgeRun();    //Bridge
            //var composite = new CompositeRun();   //Composite
            //var facade = new FacadeRun();    //Facade
            //var flyweight = new FlyweightRun();  //Flyweight
            //var proxy = new ProxyRun();  //Proxy
            //var chain = new ChainRun();  //Chain of responsibility
            //var command = new CommandRun();  //Command
            //var interpreter = new InterpreterRun();  //interpreter
            //var iterator = new IteratorRun();  //iterator
            //var mediator = new MediatorRun();  //mediator
            //var memento = new MementoRun();  //Memento
            //var state = new StateRun();   //State
            //var visitor = new VisitorRun();   //visitor
            //var template = new TemplateRun(); //Template

        }
    }
}
